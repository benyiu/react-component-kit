// Registers @testing-library/jest-dom's DOM matchers (toHaveTextContent,
// toBeVisible, …) with vitest's expect — same setup as the source repo's
// component lane.
import '@testing-library/jest-dom/vitest';
